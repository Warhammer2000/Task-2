import { PlaceholderPage } from "@/components/PlaceholderPage";

const Dashboard = () => (
  <PlaceholderPage route="/dashboard" title="Host dashboard" phase="Phase 4.4"
    description="Upcoming and past events with Going / Waitlist / Checked-in counts. CSV export. Re-balance waitlist." />
);
export default Dashboard;
