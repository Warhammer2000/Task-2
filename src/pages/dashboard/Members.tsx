import { PlaceholderPage } from "@/components/PlaceholderPage";

const Members = () => (
  <PlaceholderPage route="/dashboard/members" title="Members & invites" phase="Phase 4.6"
    description="Generate copyable invite links by role (Host or Checker). Persistent until revoked." />
);
export default Members;
