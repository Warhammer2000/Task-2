import { PlaceholderPage } from "@/components/PlaceholderPage";

const HostOnboarding = () => (
  <PlaceholderPage route="/onboarding/host" title="Become a host" phase="Phase 4.1"
    description="Self-serve registration. Pick a slug, name, contact email, and you're live as a host." />
);
export default HostOnboarding;
