import { PlaceholderPage } from "@/components/PlaceholderPage";

const MyEvents = () => (
  <PlaceholderPage route="/my/events" title="My events" phase="Phase 4.7"
    description="Aggregated events where you're a host or checker. Filter by host, date, text." />
);
export default MyEvents;
